/** Cookie que recuerda si el usuario pidió mantener la sesión entre reinicios del navegador. */
export const REMEMBER_SESSION_COOKIE = "adsme-remember";

export const REMEMBER_SESSION_ENABLED = "1";

/** Vigencia de la cookie de preferencia cuando "Recordarme" está activo (30 días). */
export const REMEMBER_SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 30;

export const MIN_PASSWORD_LENGTH = 8;
